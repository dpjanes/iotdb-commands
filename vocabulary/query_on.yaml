---
query: "on"
response: "{{ name }} is {{ value|binary('off', 'on') }}"
istate:
    iot:purpose: ":on"
    value: true
---
query: "off"
response: "{{ name }} is {{ value|binary('off', 'on') }}"
istate:
    iot:purpose: ":on"
    value: false
