---
query: "on"
response: "{{ name }} is {{ value|binary('off', 'on') }}"
match:
    iot:purpose: ":on"
    value: true
---
query: "off"
response: "{{ name }} is {{ value|binary('off', 'on') }}"
match:
    iot:purpose: ":on"
    value: false
