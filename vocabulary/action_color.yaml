---
action: 
    - "change color"
    - "set color"
argument: 
    - "red"
    - "green"
    - "blue"
    - "cyan"
    - "yellow"
    - "magenta"
    - "purple"
    - "pink"
    - "brown"
    - "white"
    - "black"
    - "gray"
do:
    iot:purpose: ":color"
header:
    one: "{{ doer_is }} changing color to {{ argument }} for {{ count }} {{ thing }}"
    response: "{{ doer_is }} changing color to {{ argument }} for {{ count }} {{ things }}"
