---
action: 
    - "set channel"
    - "change channel"
    - "switch channel"
argument: "*"
do:
    iot:purpose: ":channel"
header:
    one: "{{ doer_is }} changing channel to {{ argument }} for {{ count }} {{ thing }}"
    response: "{{ doer_is }} changing channel to {{ argument }} for {{ count }} {{ things }}"
