---
action: 
    - "set channel"
    - "change channel"
    - "switch channel"
argument: "*"
do:
    iot:purpose: ":channel"
header:
    one: "changing channel to {{ argument }} for {{ count }} {{ thing }}"
    response: "changing channel to {{ argument }} for {{ count }} {{ things }}"
