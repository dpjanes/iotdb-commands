---
action: "forget"
do:
    rule: remove
header:
    zero: "{{ doer }} found nothing to forget"
    one: "{{ doer_is }} forgetting {{ count }} {{ thing }}"
    response: "{{ doer_is }} forgetting {{ count }} {{ things }}"
